# Seeded once by container/bootstrap.sh; OMP owns the file afterwards.
# `secrets.enabled` obfuscates any op-injected key found in the environment
# before it can reach a provider.
secrets:
  enabled: true
